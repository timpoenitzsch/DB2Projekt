stages:
  - build
  - test

default:
  image: ubuntu:latest

build:
  stage: build
  script:
    - apt-get update && apt-get install -y gcc g++ make
    - mkdir build
    - cmake -H. -Bbuild
    - cd build && cmake --build .

#test:
#  stage: test
#  script:
#    - cd build && cmake --build .
#    -
